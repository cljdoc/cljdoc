{:min-bb-version "1.13.222"
 :paths ["." "script"]
 :deps {lread/status-line {:git/url "https://github.com/lread/status-line.git"
                           :sha "eace48e2e0fcc65a3986d696fb0b910fa5790a1c"}
        dev.weavejester/cljfmt {:mvn/version "0.16.5"}
        dev.nubank/docopt {:mvn/version "0.6.1-fix7"}
        etaoin/etaoin {:mvn/version "1.1.43"}
        io.github.borkdude/unused-deps {:git/sha "c6673b5549d02fa131d21b61695e283b18124b59"}
        org.clj-commons/pretty {:mvn/version "3.9.0"}
        io.github.babashka/esbuild {:mvn/version "0.1.1"}
        ;; reference as a git dep so that esbuild can easily find squint assets
        ;; using git/sha without git/tag to get some unreleased fixes
        io.github.squint-cljs/squint {:git/sha "639177e6a006d333aee9ede8b4b148c1ae802f1f"}}
 :pods {org.babashka/fswatcher {:version "0.0.7"}}
 :tasks
 {;; setup
  :requires ([babashka.fs :as fs]
             [babashka.process :as p]
             [clojure.string :as str]
             [build-shared :as bs]
             [lread.status-line :as status]
             [version])
  :init (defn clj [args]
           (apply clojure args *command-line-args*))
  :enter (let [{:keys [name task-decoration] :as f} (current-task)]
           (when-not (= :none task-decoration)
             (status/line :head "TASK %s" name)))
  :leave (let [{:keys [name task-decoration] :as f} (current-task)]
           (when-not (= :none task-decoration)
            (status/line :detail "\nTASK %s done." name)))
  :cli helper.cli/base-opts

  ;; commands
  clean                {:exec-fn clean/task                 :doc "Delete all build work"}
  download-deps        {:exec-fn download-deps/task         :doc "Download clojure deps"}
  dev                  {:exec-fn dev-repl/task              :doc "Fire up a REPL for dev"
                        :depends [compile-js compile-java]}
  deps-js              {:exec-fn deps-js/task               :doc "Bring down js deps"}
  compile-js           {:exec-fn compile-js/task            :doc "Compile front end sources"
                        :depends [deps-js]}
  test-js              {:exec-fn compile-js/task            :doc "Run front-end tests"
                        :exec-args {:test true}
                        :depends [deps-js]}
  compile-java         {:exec-fn compile-java/task          :doc "Compile java sources"}
  server               {:task (clj "-M:cli run")            :doc "Launch cljdoc server"
                        :depends [compile-js compile-java]}
  ingest               {:task (clj "-M:cli ingest")         :doc "Ingest docs locally for testing"
                        :depends [compile-js compile-java]}
  offline-docset       {:task (clj "-M:cli offline-docset") :doc "Create offline docset for built lib"
                        :depends [compile-js compile-java]}
  test                 {:task (clj "-M:test")               :doc "Run tests"
                        :depends [compile-js compile-java]}
  lint                 {:exec-fn lint/task                  :doc "lint source code using clj-kondo"}
  eastwood             {:task (clj "-M:test:eastwood")      :doc "lint source code using eastwood"
                        :depends [compile-java]}
  code-format          {:cmd {"check" {:exec-fn code-format/check}
                              "fix"   {:exec-fn code-format/fix}}
                        :exec-fn code-format/check
                        :doc "check or fix whitespace formatting"}
  current-version      {:task version/-main                 :doc "print current version"
                        :task-decoration :none}
  package              {:exec-fn package/task               :doc "Zip up cljdoc for docker"}
  docker-image         {:doc "Create docker image"
                        :depends [package]
                        :task (shell {:dir "ops/docker"}
                               "bb image.clj" (fs/absolutize "target/build/cljdoc.zip") (version/version))}
  update-github-emojis {:exec-fn update-github-emojis/task  :doc "Update the github emojis used in cljdoc article markdown"}
  outdated             {:exec-fn outdated/task :doc "report on outdated Clojure and JavaScript dependencies"
                        :depends [deps-js]}
  doc-update-readme    {:exec-fn doc-update-readme/task     :doc "honour our contributors in README" }
  check-contributors   {:exec-fn check-contributors/task    :doc "who'd we miss?"}
  unused-deps
  {:doc "find unused dependencies"
   :requires ([clj-commons.format.table :as table])
   :task (let [;; manually update explanations as needed
               explanations {'org.asciidoctor/asciidoctorj "actual API is in dep org.asciidoctor/asciidoctorj-api which we do not explicity depend on"
                             'dev.weavejester/ragtime "ragtime API is in its dependencies"}
               unused-deps (->> (exec 'borkdude.unused-deps/unused-deps)
                                :unused-deps
                                sort
                                (mapv (fn [d] (-> {:dep (str d)}
                                                  (assoc :explanation (get explanations (first d)))))))]
           (status/line :detail "Dependencies that do not seem to be referenced by code")
           (status/line :detail "NOTE: Update explanation in bb.edn as you see fit")
           (status/line :detail "")
      (table/print-table [{:key :dep         :title-align :left :title "Dependency"  :align :left}
                          {:key :explanation :title-align :left :title "Explanation" :align :left}]
                         unused-deps)
      (status/line :detail "Total unused deps with no explanation: %d" (count (filter (fn [r] (-> r :explanation nil?)) unused-deps))))}
  nvd-scan
  {:doc "Scan Clojure deps for vulnerabilities"
   :task (shell {:dir "./modules/nvd-scan"} "bb nvd-scan")}

  deploy
  {:doc "ops: Deploy to Production (run by CircleCI)"
   :task (let [cljdoc-version (-> (shell {:out :string} "bb current-version") :out str/trim)]
           (status/line :detail "Deploying cljdoc version %s" cljdoc-version)
           (apply shell {:dir "ops/exoscale/deploy"}
                  "clojure -M -m cljdoc.deploy deploy -t" cljdoc-version
                  *command-line-args*))}}}
